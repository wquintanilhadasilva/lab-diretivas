import { BotaoDiretivaDirective } from './botao-diretiva.directive';

describe('BotaoDiretivaDirective', () => {
  it('should create an instance', () => {
    const directive = new BotaoDiretivaDirective();
    expect(directive).toBeTruthy();
  });
});
